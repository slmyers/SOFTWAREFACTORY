import pytest


@pytest.mark.skip(reason="CLI `main.py resume` not implemented in workspace — placeholder test for Issue #5")
def test_cli_resume_restores_state():
    """Placeholder: when implemented, `python main.py resume --thread_id <id>` should restore AgentState."""
    pass
